import WeightedChains.MainTheorem

/-!
# Comparator solution

The proved counterpart of `Challenge.lean`. Importing
`WeightedChains.MainTheorem` puts the definitions restated in the challenge and
the proof of `WeightedChains.main_cardinality_and_uniqueness` into this
environment, so no declaration is repeated here.

See `comparator.json` for the [Comparator](https://github.com/leanprover/comparator)
configuration.
-/
